CREATE TABLE VISITOR
(
    VISITORID NUMBER(6,0) PRIMARY KEY,
    FIRST_NAME VARCHAR2(26),
    LAST_NAME VARCHAR2(26),
    EMAIL VARCHAR(26) CHECK(REGEXP_LIKE(EMAIL, '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,4}$'))
)

COMMIT;
